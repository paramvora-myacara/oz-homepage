import '../globals_dashboard.css';

export const metadata = {
  title: 'OZ Dashboard',
  description: 'Opportunity Zone Investment Dashboard',
};

export default function DashboardLayout({ children }) {
  return <>{children}</>;
}